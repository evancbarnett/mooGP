# Dependencies for the `.venv` used by MOOGP, MOGP, and LCGP.
# OILMM and PUQ use their own virtualenvs (see experiments/run_guide.md).
#
# Pins are deliberate: the TF 2.16 / numpy 1.x / gpflow 2.10 / lcgp 0.2 stack
# is the configuration the benchmarks were run against, and TF 2.16 requires
# numpy<2. Bumping any of these is a coordinated change.

# Core numerical stack
numpy==1.26.4
scipy==1.16.2

# MOOGP — autodiff for the marginal likelihood / gradients
autograd==1.8.0

# Plotting — illustration scripts in moogp/ and experiments/plot_benchmark_boxplots.py
matplotlib==3.10.7

# LCGP comparator (experiments/benchmark_lib.py::_fit_lcgp)
# gpflow provides the Scipy optimizer LCGP fits with.
tensorflow==2.16.2
tensorflow-probability==0.24.0  # last TFP that supports TF 2.16 (>=0.25 needs TF>=2.18)
tf-keras==2.16.0           # Keras-2 shim; gpflow uses the legacy Keras API
gpflow==2.10.0
lcgp==0.2.0

# Tests (moogp/tests, experiments/tests)
pytest==9.0.1
