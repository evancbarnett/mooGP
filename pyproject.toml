[project]
name = "moogp"
version = "0.9.0"
description = " "
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "autograd>=1.6.0",
    "numpy>=2.0.0",
    "scipy>=1.10.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
